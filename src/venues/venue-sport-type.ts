/** Allowed venue sport tags (stored as TEXT[] in PostgreSQL). */
export enum VenueSportType {
  Football = 'football',
  Basketball = 'basketball',
  Tennis = 'tennis',
  Volleyball = 'volleyball',
  Generic = 'generic',
}
