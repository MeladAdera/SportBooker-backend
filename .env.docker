# Defaults for docker-compose. Override via environment in docker-compose.yml if needed.
PORT=3000
TENANT_HOST_SUFFIX=localhost:3000
DB_HOST=db
DB_PORT=5432
DB_NAME=sportbooker
DB_USER=postgres
DB_PASSWORD=postgres
